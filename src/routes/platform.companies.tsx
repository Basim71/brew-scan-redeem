import { createFileRoute } from "@tanstack/react-router";
import { useEffect,useState } from "react";
import { Building2, Search, ShieldCheck, ShieldOff } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/platform/companies" as any)({component:Companies});
type Company={id:string;name_ar:string|null;name_en:string|null;organization_code:string;status:string;created_at:string};
function Companies(){const [rows,setRows]=useState<Company[]>([]);const [q,setQ]=useState("");useEffect(()=>{void (async()=>{const {data}=await (supabase as any).from("organizations").select("id,name_ar,name_en,organization_code,status,created_at").order("created_at",{ascending:false});setRows(data??[])})()},[]);const filtered=rows.filter(x=>`${x.name_ar} ${x.name_en} ${x.organization_code}`.toLowerCase().includes(q.toLowerCase()));return <div className="platform-page" dir="rtl"><header className="platform-page-header"><div><span>Organizations</span><h1>الشركات</h1><p>إدارة جميع الشركات المشتركة في منصة KOB.</p></div><button className="platform-primary-button"><Building2/> إنشاء شركة</button></header><div className="platform-toolbar"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث بالاسم أو كود الشركة"/></label></div><div className="platform-table-wrap"><table className="platform-table"><thead><tr><th>الشركة</th><th>الكود</th><th>الحالة</th><th>تاريخ الإنشاء</th><th>الدعم</th></tr></thead><tbody>{filtered.map(c=><tr key={c.id}><td><strong>{c.name_ar||c.name_en}</strong><small>{c.name_ar&&c.name_en?c.name_en:""}</small></td><td><code>{c.organization_code}</code></td><td><span className={`platform-status ${c.status}`}>{c.status==="active"?<ShieldCheck/>:<ShieldOff/>}{c.status==="active"?"نشطة":"موقوفة"}</span></td><td>{new Date(c.created_at).toLocaleDateString("ar-SA")}</td><td><button className="platform-secondary-button">طلب جلسة</button></td></tr>)}</tbody></table>{!filtered.length&&<div className="platform-empty">لا توجد شركات مطابقة.</div>}</div></div>}
