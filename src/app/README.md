# app/

TanStack Start's bootstrap files (`router.tsx`, `start.ts`, `server.ts`,
`styles.css`) live at `src/` because the framework auto-discovers them
there. Treat `src/app/` as the conceptual "application shell" folder and
add any future app-wide composition roots here.