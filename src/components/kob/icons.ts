/**
 * Single icon vocabulary for KOB. Every surface imports icons from here
 * so the same concept always renders the same glyph (lucide, 1.75 stroke).
 */
export {
  // navigation
  LayoutDashboard as IconDashboard,
  Users as IconCustomers,
  Ticket as IconCoupons,
  Coffee as IconDrinks,
  CreditCard as IconPlans,
  Store as IconBranches,
  UserCog as IconStaff,
  BarChart3 as IconReports,
  Settings as IconSettings,
  LifeBuoy as IconSupport,
  Building2 as IconCompany,
  ShieldCheck as IconPlatform,
  ScanLine as IconScan,
  // actions
  Plus as IconAdd,
  Pencil as IconEdit,
  Trash2 as IconDelete,
  Search as IconSearch,
  Filter as IconFilter,
  Download as IconExport,
  RefreshCw as IconRefresh,
  X as IconClose,
  Check as IconCheck,
  ChevronLeft as IconPrev,
  ChevronRight as IconNext,
  ChevronDown as IconExpand,
  LogOut as IconSignOut,
  Languages as IconLanguage,
  // status
  Clock as IconPending,
  CircleCheck as IconApproved,
  CircleX as IconRejected,
  TriangleAlert as IconWarning,
  Info as IconInfo,
  Inbox as IconEmpty,
  Loader2 as IconSpinner,
} from "lucide-react";
