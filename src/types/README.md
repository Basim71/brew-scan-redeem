# types/

Reserved for cross-cutting TypeScript types. Feature-local types live
next to their feature (e.g. `src/features/drinks/types.ts`), and the
Supabase-generated database types stay in `src/integrations/supabase/`.